<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WAMFF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:WAMFF.Components"
    mc:Ignorable="d"
    Title="WAMFF">

    <Grid Name="MainGrid" Background="Transparent">
        <Grid.Resources>
            <Style TargetType="Border" x:Key="ContainerStyle">
                <Setter Property="BorderBrush" Value="#1cffffff" />
                <Setter Property="BorderThickness" Value="2" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="Padding" Value="10" />
                <Setter Property="Background" Value="#aa0c0c0e" />
            </Style>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <components:TitleBar Grid.Row="0" />

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="15" />
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="15" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="15" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="15" />
                <RowDefinition Height="*" />
                <RowDefinition Height="15" />
            </Grid.RowDefinitions>

            <Border Grid.Column="1" Grid.Row="0" Style="{StaticResource ContainerStyle}">
                <components:CategoryDisplayCollection />
            </Border>

            <Border Grid.Column="1" Grid.Row="2" Style="{StaticResource ContainerStyle}">
                <components:TagDisplayCollection />
            </Border>

            <Border Grid.Column="3" Grid.RowSpan="3" AllowDrop="True" DragOver="{x:Bind ViewModel.CanDrop }" Drop="{x:Bind ViewModel.FileDropped}" Style="{StaticResource ContainerStyle}">
                <components:FileDisplayColletion />
            </Border>
        </Grid>
    </Grid>
</Window>