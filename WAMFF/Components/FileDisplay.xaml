<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WAMFF.Components.FileDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:WAMFF.Components"
    xmlns:converters="using:WAMFF.Core.Converters"
    xmlns:viewmodels="using:WAMFF.Core.ViewModels"
    d:DataContext="{d:DesignInstance Type=components:FileDisplay}"
    x:Name="RootControl"
    mc:Ignorable="d">

    <StackPanel>
        <Grid Padding="0 10 0 10" DoubleTapped="{x:Bind ViewModel.OpenDoubleTap, Mode=OneWay}" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="45" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>

            <Grid.ContextFlyout>
                <MenuFlyout x:Name="FlyoutMenu">
                    <MenuFlyoutItem Text="Open" Command="{x:Bind ViewModel.OpenFileCommand, Mode=OneWay }">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xeaf3;" FontFamily="{StaticResource MaterialSymbolsFilled}" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem Text="Open with" Command="{x:Bind ViewModel.OpenWithCommand, Mode=OneWay}">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xe89f;" FontFamily="{StaticResource MaterialSymbolsFilled}" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem Text="Open in VSCode" x:Name="VSCodeFlyout" Command="{x:Bind ViewModel.OpenWithVsCodeCommand, Mode=OneWay}">
                        <MenuFlyoutItem.Icon>
                            <ImageIcon Source="/Assets/Icons/vscode.png" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem Text="Show in Explorer" Command="{x:Bind ViewModel.OpenInExplorerCommand, Mode=OneWay}">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xe2c8;" FontFamily="{StaticResource MaterialSymbolsFilled}" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    
                    <MenuFlyoutSeparator />
                    
                    <MenuFlyoutItem Text="Change Category" Command="{x:Bind ViewModel.ShowCategoryDialogCommand, Mode=OneWay}">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xe866;" FontFamily="{StaticResource MaterialSymbolsFilled}" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>

                    <MenuFlyoutItem Text="Rename" Command="{x:Bind ViewModel.ShowRenameDialogCommand, Mode=OneWay}">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xe3c9;" FontFamily="{StaticResource MaterialSymbolsFilled}" />
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    
                    <MenuFlyoutSeparator />
                    
                    <MenuFlyoutItem Text="Delete" Command="{x:Bind ViewModel.ShowDeleteDialogCommand, Mode=OneWay}" Background="Red">
                        <MenuFlyoutItem.Icon>
                            <FontIcon Glyph="&#xe872;" FontFamily="{StaticResource MaterialSymbolsFilled}"/>
                        </MenuFlyoutItem.Icon>
                    </MenuFlyoutItem>
                    
                </MenuFlyout>
            </Grid.ContextFlyout>

            <Image Grid.Column="0" Source="{x:Bind ViewModel.File.Details.FileIcon, Mode=OneWay}" Width="45" Height="45" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Text="{x:Bind ViewModel.File.Details.Name, Mode=OneWay}" VerticalAlignment="Center" Margin="10 0 0 0" />

            <AppBarSeparator Grid.Column="2" />
            <Button Grid.Column="3"  Command="{x:Bind ViewModel.ShowCategoryDialogCommand, Mode=OneWay}" Margin="10 0 0 0">
                <FontIcon Glyph="&#xe866;" FontSize="25" FontFamily="{StaticResource MaterialSymbolsFilled}" Foreground="{x:Bind converters:HexConverter.ConvertHex(ViewModel.Category.Color), Mode=OneWay}" />
            </Button>

            <AppBarSeparator Grid.Column="4" />
            <Button Grid.Column="5" Margin="10 0 0 0" Command="{x:Bind ViewModel.ShowTagsDialogCommand, Mode=OneWay}">
                <StackPanel Orientation="Horizontal">
                    <FontIcon Glyph="&#xefb7;" FontSize="25" FontFamily="{StaticResource MaterialSymbolsFilled}" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}" />
                    <TextBlock  Text="{x:Bind ViewModel.File.Stats.Tags.Count, Mode=OneWay}" VerticalAlignment="Center" Margin="10 0 0 0" />
                </StackPanel>
            </Button>

            <AppBarSeparator Grid.Column="6" />
            <TextBlock Grid.Column="7" Text="{x:Bind ViewModel.File.Details.FileType, Mode=OneWay}" VerticalAlignment="Center" Margin="10 0 0 0" />

            <AppBarSeparator Grid.Column="8" />
            <TextBlock Grid.Column="9" Text="{x:Bind ViewModel.File.Details.Extension, Mode=OneWay}" VerticalAlignment="Center" Margin="10 0 0 0" />

            <AppBarSeparator Grid.Column="10" />
            <TextBlock Grid.Column="11" Text="{x:Bind ViewModel.File.Details.CreatedDate, Mode=OneWay}" VerticalAlignment="Center" Margin="10 0 0 0" />

            <AppBarSeparator Grid.Column="12" />
            <TextBlock Grid.Column="13" Text="{x:Bind ViewModel.File.Details.LastModifiedDate, Mode=OneWay}" VerticalAlignment="Center" Margin="10 0 0 0" />

            <AppBarSeparator Grid.Column="14" />
            <TextBlock Grid.Column="15" Text="{x:Bind ViewModel.File.Details.HumanReadableSize, Mode=OneWay}" VerticalAlignment="Center" Margin="10 0 0 0" />
        </Grid>

        <ContentDialog x:Name="RenameDialog" PrimaryButtonText="Accept" CloseButtonText="Cancel" Title="Rename file">
            <StackPanel>
                <TextBox Text="{x:Bind ViewModel.RenameText, Mode=TwoWay}" />
            </StackPanel>
        </ContentDialog>

        <ContentDialog x:Name="CategoryDialog" PrimaryButtonText="Accept" CloseButtonText="Cancel" Title="Change category">
            <StackPanel>
                <AutoSuggestBox Text="{x:Bind ViewModel.Category.Name, Mode=OneWay}" TextChanged="{x:Bind ViewModel.UpdateSuggestionBox, Mode=OneWay}" QuerySubmitted="{x:Bind ViewModel.CategorySuggestionQuery, Mode=OneWay}" />
            </StackPanel>
        </ContentDialog>

        <ContentDialog x:Name="TagsDialog" PrimaryButtonText="Save" CloseButtonText="Cancel" Title="Change tags">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <AutoSuggestBox Grid.Row="0" Margin="0 0 0 10" Text="{x:Bind ViewModel.TagSuggestionBoxText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" TextChanged="{x:Bind ViewModel.TagUpdateSuggestionBox, Mode=OneWay}" QuerySubmitted="{x:Bind ViewModel.TagSuggestionQuery, Mode=OneWay}" />

                <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.AssignedTags, Mode=OneWay}" SelectionMode="None" MinHeight="50">
                    <ListView.ItemTemplate>
                        <DataTemplate  x:DataType="viewmodels:TagsUpdateContext">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{x:Bind Tag, Mode=OneWay}" VerticalAlignment="Center" />
                                <Button Grid.Column="1" Command="{x:Bind RemoveTagCommand, Mode=OneWay}" HorizontalAlignment="Right">
                                    <FontIcon Glyph="&#xe872;" FontFamily="{StaticResource MaterialSymbolsFilled}" Foreground="{ThemeResource AccentAAFillColorDefaultBrush}" />
                                </Button>

                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </ContentDialog>

        <ContentDialog x:Name="DeleteDialog" PrimaryButtonText="Delete" CloseButtonText="Cancel" Title="Delete file">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Are you sure you want to delete file: '" />
                <TextBlock Text="{x:Bind ViewModel.File.Details.Name, Mode=OneWay}" />
                <TextBlock Text="'?" />
            </StackPanel>
        </ContentDialog>
    </StackPanel>
</UserControl>