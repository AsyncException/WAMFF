<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WAMFF.Components.TitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WAMFF.Components"
    xmlns:viewmodels="using:WAMFF.Core.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <viewmodels:TitleBarViewModel x:Name="ViewModel" />
    </UserControl.Resources>

    <Grid>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" MinWidth="48" />
                <ColumnDefinition Width="4*" MinWidth="220" />
                <ColumnDefinition Width="*" MinWidth="48" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="140" />
            </Grid.ColumnDefinitions>

            <Image Source="/Assets/StoreLogo.png" Grid.Column="1" Width="48" Height="48" Margin="8,0,4,0" />
            <TextBlock Text="Where are my files!" Style="{StaticResource CaptionTextBlockStyle}" Grid.Column="2" VerticalAlignment="Center" />
            <TextBox x:Name="SearchBox" Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Column="4" PlaceholderText="Search" VerticalAlignment="Center" MaxWidth="600" Background="#aa0c0c0e" />
            <Button Grid.Column="6" Command="{x:Bind ViewModel.OpenSettingsCommand}" >
                <FontIcon Glyph="&#xe8b8;" FontFamily="{StaticResource MaterialSymbolsFilled}" />
            </Button>

        </Grid>
    </Grid>
</UserControl>